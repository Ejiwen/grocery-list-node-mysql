/*

var controllers = require('./controllers');

var express = require('express');
var router = express.Router();

rooter.get('/getAll', controllers.getAll);

rooter.post('/postItem', controllers.postItem);

rooter.delete(`/items/:id`, controllers.deleteItem);

rooter.put(`/item/:id:updatItem`, controllers.changeItem);

module.exports = router;

*/